node_modules/
.project_id
.github_repo
.env
.env.*
.grok/
.vercel/
.DS_Store
Thumbs.db
dist/
data/relay-secrets.json
data/relay-room.json
data/relay-update.log
data/*.tmp
data/*.transaction
# Last-good / corrupt backups from persistPair + loadPersisted (write-atomic).
data/*.good
data/*.bad
data/*.transaction.bad
attachments/
artifacts/
screenshots/app-builder-*


# Room working-set copies. Stock lives in data/library/.
data/drivers/*.json

# C1 venue TLS PEMs (never commit keys)
data/tls/

# Uploaded panel Image widget binaries. Stock keep via .gitkeep.
data/media/**
!data/media/.gitkeep
